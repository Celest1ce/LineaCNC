# Copy this file to .env.docker and adjust values if needed.

# MariaDB credentials
MARIADB_ROOT_PASSWORD=change_me_root
DB_NAME=lineacnc
DB_USER=lineacnc
DB_PASS=lineacncpassword

# API app settings
SESSION_SECRET=change_me_session_secret
