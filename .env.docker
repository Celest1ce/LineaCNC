# Docker Compose environment for local stack
# Rotate these values for non-local environments.

MARIADB_ROOT_PASSWORD=change_me_root
DB_NAME=lineacnc
DB_USER=lineacnc
DB_PASS=lineacncpassword
SESSION_SECRET=change_me_session_secret
